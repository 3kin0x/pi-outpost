## 1. Move the version

- [x] 1.1 Raise `@earendil-works/pi-coding-agent` from `^0.84.3` to `^0.84.4` in `server/package.json` and `cli/package.json`, run `npm install`, and verify `package-lock.json` resolves 0.84.4 with a new integrity hash and that no other dependency moved in the same diff. Done, with a finding the task's last clause forced into the open: the lockfile diff is large (1314/1541 lines, 89 entries added and 107 removed), because 0.84.4 lets npm hoist pi's transitive dependencies out of `node_modules/@earendil-works/pi-coding-agent/node_modules/` to the top level — 135 nested entries fall to 28. Of our own declared dependencies, exactly one moved: pi itself. Nothing was removed from outside pi's subtree. The churn is pi's own graph, and it is caused by this bump rather than by a stale lock: `npm install` on untouched `HEAD` produces an empty lockfile diff. Re-running the install reproduces the new lock byte for byte.
- [x] 1.2 Verify the installed package is the one the pin now names, and that no second copy is nested elsewhere in `node_modules`. Done: one copy on disk, reading 0.84.4. The probe this task named does not work — the package's `exports` does not expose `./package.json`, so `require(...)` raises `ERR_PACKAGE_PATH_NOT_EXPORTED`; the version was read from the file directly instead. That is a fault in the task as written, not in the install.

## 2. Check the three routes pi reaches this project by

- [x] 2.1 Embedded: run the server suite and the UI suite, and verify both pass with nothing skipped — the embedded SDK session is what most of the server suite exercises. Done: server 1599 passed, 0 failed, 0 skipped, 0 cancelled; UI 63 files / 1346 tests passed with the coverage gate held (functions 93.36% against a 93% floor, lines 94.04%, branches 87.66%).
- [x] 2.2 RPC: start a server in `rpc` mode against a real `pi --mode rpc` child and drive one turn that uses a tool, then read the transcript back and verify the tool call and its result are in order and the turn completed. A green exit code is not the check; 0.84.4 changed how extension messages are ordered around tool results. Done against `node_modules/.bin/pi` at 0.84.4, one turn asking the agent to read a file and repeat it. The turn reached `agent_end`, `tool_start` and `tool_end` both arrived, and the transcript reads `user(text) -> assistant(text+toolCall) -> toolResult(text) -> assistant(text)` — the call and its result adjacent and in order. The JSONL ends with a newline, which is the condition the resumed-session corruption fix in this release turns on. Worth recording: the `pi` on this machine's `PATH` is 0.84.2, so an operator running `mode: "rpc"` uses their own binary and not the pinned one.
- [x] 2.3 SEA: build the self-contained executable and run it, and verify it starts, serves the interface, and loads an extension — the bundle is where an import gap surfaces first, and it is the artifact the beta ships as. Done via `build-exe`, the path CI uses, which invokes the result before reporting success. It reports `0.17.0-beta.1`, serves the interface and its embedded assets (190 of them; `/`, `/manifest.webmanifest`, `/icon-192.png` all 200), resolves a model, and loads a TypeScript extension from disk through the SDK's jiti loader — the fixture printed from inside the SEA. The bundler also reported `the SDK already detects a Node SEA binary itself (pi#8237) — nothing to patch`, so the guard behaves the same on 0.84.4. One thing to know for anyone repeating this on macOS: `npm run build:sea` produces an unsigned Mach-O that dies in dyld with SIGSEGV before any JS runs — that `.exe` is the Windows artifact, and `build-exe` detects the condition and falls back to the blob by itself.

## 3. Confirm nothing was carried along

- [x] 3.1 Verify the comments in `cli/scripts/build.mjs`, `server/scripts/build-sea.mjs` and `server/test/seaExtensionImports.test.ts` still describe the truth: 0.84.3 is where pi#8237 was fixed upstream, and the guard they explain is still needed. Leave them as they are if so. Done, left untouched. Each one checks the SDK's shape explicitly rather than assuming it, and on 0.84.4 the build reports `nothing to patch` — the upstream fix is still there. `seaExtensionImports.test.ts` asserts against the SDK as installed and passed in the suite above, which is the check that would catch the anchors moving.
- [x] 3.2 Verify no upstream API new in 0.84.4 was adopted — no reference to `ui_prompt_start`, `ui_prompt_end` or `clear_queue` appears in the diff — and that the change touches only the two manifests and the lockfile. Done: none of those names, nor the other 0.84.4 additions, appears anywhere in our source. Outside the lockfile the diff is two lines, one per manifest.
- [x] 3.3 Read the version pi-outpost reports in the interface and verify it names 0.84.4, since that string is what tells an operator which SDK they are actually on. Done, after fixing what it exposed. The executable already reported it (`piSdk: "0.84.4"`); a server run from source reported `"dev"`, because `PI_SDK_VERSION` was a bundle-time define with a literal fallback. It now reads the installed package: a `tsx watch` run in the shape `npm run dev` uses reports `{piOutpost: "dev", piSdk: "0.84.4"}`, and a rebuilt executable still reports `{piOutpost: "0.17.0-beta.1", piSdk: "0.84.4"}` with its extension still loading, so the define still wins where it exists. `piOutpost` stays `"dev"` from source deliberately — that run is on no released version.

## 3bis. Name the SDK in every run shape

- [x] 3b.1 Read the version from the installed package when the bundle-time define is absent, in its own module rather than in `index.ts`, and verify the walk-up finds the package that bears the name rather than the nearest manifest — `server/src/piSdkVersion.ts`, with 7 tests in `server/test/piSdkVersion.test.ts` covering the real install, a nested impostor manifest, a manifest with no version, an absent package, and a resolver that throws.
- [x] 3b.2 Verify at the boundary, not only in the unit: drive a real server and read `versions` off the wire, in both shapes it can take — `server/test/versionsWire.test.mjs`, 2 tests. A source run names its SDK and no child; an RPC server names the child and omits the SDK, since naming both would tell the operator two different things answer their prompts.
- [x] 3b.3 Write the contract down, since observable behaviour changed: `skip_specs` removed and `specs/api/spec.md` added with `TheSnapshotNamesWhatAnswersPrompts` and four scenarios.
- [x] 3b.4 Write the scenario-to-test matrix for the new delta with assertion-level evidence, and leave no scenario partial or uncovered. Done in `scenario-coverage.md`: four scenarios enumerated with `rg`, all four `covered`. Writing it forced two gaps into the open rather than papering over them. `ADistributedBuildNamesItsSdk` was only ever asserted against the text of one build script, and the bundle-time define is unobservable from a source run — so `server/test/versionsBundled.test.mjs` now bundles the real `index.ts` the way `cli/scripts/build.mjs` does, substitutes a sentinel, starts it and reads the version off the wire; the harness gained an `entry` option to run an already-bundled server. And the static guard covered one define site of three: `cli/scripts/build.mjs` has two of its own, either of which could have hardcoded a literal unseen. It now reads both scripts, checks every site and asserts the count. A Codex review of the matrix then refused two of its own rows, correctly: the executable's bundle is a *different* esbuild build from the npm one, and the unreadable-version row was proved only by injection. Both are now driven at the wire — the no-externals bundle run from outside the repo, and the same bundle with no defines at all, which cannot resolve the SDK and must say `dev` rather than name a version it never read.


## 4. Land it

- [x] 4.1 Run lint, typecheck, and the full suites. Done after the version-reporting fix and after the coverage work that followed it: lint and typecheck clean across every workspace; server 1611 passed / 0 failed / 0 skipped / 0 cancelled (the run in 2.1 predates the fix and read 1599 — the twelve added are seven unit, two at the wire, three bundled); UI 63 files / 1346 tests passed. The server suite was re-run *after* `harness.mjs` changed, since 27 test files import it. Still open: open the change and verify CI is green on both platforms before merging — the Windows runner is the one this release has a fix for.
- [x] 4.2 Run `openspec validate bump-pi-coding-agent-to-0-84-4 --strict` and the Playwright suite, which the added delta now requires. Done: the change validates, and Playwright is 46 passed after a full `npm run build` — the bench serves `dist/`, so an unbuilt fix would have been invisible.
